[package]
name = "justypdocs"
version = "0.0.1"
entrypoint = "src/lib.typ"
authors = ["rtsang"]
license = "MIT"
description = "A Typst static documentation-site template inspired by Just the Docs."
compiler = "0.15.0"
